# Authentication

Users sign in via OAuth 2.0. The session is stored as a JWT cookie.

## OAuth flow

1. Redirect to provider
2. Handle callback
3. Exchange code for token

## Sessions

Logout via [[index#sections]].
